[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'gunicorn config.wsgi:application --bind 0.0.0.0:${PORT:-8000} --workers 2'"
healthcheckPath = "/api/v1/health/"
restartPolicyType = "ON_FAILURE"